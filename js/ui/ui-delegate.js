// UI Object - Delegates to standalone functions in ui/ modules
// Load this AFTER all ui/*.js files
const UI = {
    // State properties
    _seatAbort: null,
    longPressTimer: null,
    _closeMenuFn: null,
    _autoDrawInterval: null,
    _autoDrawRunning: false,
    blacklistSearch: null,
    whitelistSearch: null,
    _recommendations: [],
    _customAlgorithms: {},
    _customAlgorithm: null,
    _fullSearchFilter: 'all',

    // Delegated methods
    init() { return init(); },
    renderClassroom() { return renderClassroom(); },
    generateUniqueRandomNumbers(n) { return generateUniqueRandomNumbers(n); },
    generateDrawOrder() { return generateDrawOrder(); },
    checkAisles() { return checkAisles(); },
    updateEmptyState() { return updateEmptyState(); },
    bindSeatEvents(sig) { return bindSeatEvents(sig); },
    handleSeatClick(seat) { return handleSeatClick(seat); },
    doSwap(s1, s2) { return doSwap(s1, s2); },
    swapSeats(s1, s2) { return swapSeats(s1, s2); },
    updateSeatDisplay(seat) { return updateSeatDisplay(seat); },
    clearSeatName(el) { return clearSeatName(el); },
    clearSelection() { return clearSelection(); },
    toggleLunch(seat) { return toggleLunch(seat); },
    clearSeat(seat) { return clearSeat(seat); },
    disableSeat(seat) { return disableSeat(seat); },
    enableSeat(seat) { return enableSeat(seat); },
    disableColumn(col) { return disableColumn(col); },
    enableColumn(col) { return enableColumn(col); },
    seatLabel(seat) { return seatLabel(seat); },
    fillSeat(student) { return fillSeat(student); },
    togglePin(seat) { return togglePin(seat); },
    animateSeatsIn() { return animateSeatsIn(); },
    navigateSeats(direction) { return navigateSeats(direction); },
    toggleBatchSeat(seat) { return toggleBatchSeat(seat); },
    enterBatchMode() { return enterBatchMode(); },
    exitBatchMode() { return exitBatchMode(); },
    resetDraw() { return resetDraw(); },
    startAutoDraw() { return startAutoDraw(); },
    stopAutoDraw() { return stopAutoDraw(); },
    _doDrawNextFromSequence() { return _doDrawNextFromSequence(); },
    doDrawNext() { return doDrawNext(); },
    getHeatmapColor(score, alpha) { return getHeatmapColor(score, alpha); },
    getHeatmapScore(student) { return getHeatmapScore(student); },
    renderHeatmap() { return renderHeatmap(); },
    clearHeatmap() { return clearHeatmap(); },
    showStudentInfo(seat) { return showStudentInfo(seat); },
    renderPool() { return renderPool(); },
    showStats() { return showStats(); },
    renderPoolSearchDropdown(query) { return renderPoolSearchDropdown(query); },
    showStudentDetailFromPool(student) { return showStudentDetailFromPool(student); },
    performFullStudentSearch(query, filter) { return performFullStudentSearch(query, filter); },
    generateRecommendations() { return generateRecommendations(); },
    buildTutoringChain() { return buildTutoringChain(); },
    calcTotalInfluence(s) { return calcTotalInfluence(s); },
    _calcWithCustomAlgo(s) { return _calcWithCustomAlgo(s); },
    enhancedPeerInfluence(s1, s2, seat1, seat2) { return enhancedPeerInfluence(s1, s2, seat1, seat2); },
    calcRowBalance(s) { return calcRowBalance(s); },
    calcLunchPenalty(s) { return calcLunchPenalty(s); },
    calcRelationshipInfluence(s) { return calcRelationshipInfluence(s); },
    getNeighbors(seat) { return getNeighbors(seat); },
    showRecommendations() { return showRecommendations(); },
    renderRecommendations() { return renderRecommendations(); },
    highlightSeat(seat) { return highlightSeat(seat); },
    clearRecommendHighlights() { return clearRecommendHighlights(); },
    acceptRecommendation(idx) { return acceptRecommendation(idx); },
    skipRecommendation(idx) { return skipRecommendation(idx); },
    applyAllRecommendations() { return applyAllRecommendations(); },
    renderCustomAlgoList() { return renderCustomAlgoList(); },
    _downloadAlgoTemplate() { return _downloadAlgoTemplate(); },
    peerInfluence(s1, s2, context) { return peerInfluence(s1, s2, context); },
    getScore(student) { return getScore(student); },
    _createSimWorker() { return _createSimWorker(); },
    _createProgressPanel() { return _createProgressPanel(); },
    _updateProgress(c, t, s) { return _updateProgress(c, t, s); },
    _updateAlerts(pf, n) { return _updateAlerts(pf, n); },
    _showSimConfig() { return _showSimConfig(); },
    _onSimulationComplete(sf, pf, n, st, r) { return _onSimulationComplete(sf, pf, n, st, r); },
    _calcGini(v) { return _calcGini(v); },
    _generateSuggestions(p, n) { return _generateSuggestions(p, n); },
    _showMonteCarloDashboard(sf, se, ap, n, e, g, s) { return _showMonteCarloDashboard(sf, se, ap, n, e, g, s); },
    _buildHeatmapTab(sf, se, n) { return _buildHeatmapTab(sf, se, n); },
    _buildMatrixTab(sf, n) { return _buildMatrixTab(sf, n); },
    _buildPairsTab(ap, n) { return _buildPairsTab(ap, n); },
    _buildSuggestTab(s) { return _buildSuggestTab(s); },
    async runMonteCarloSimulation(n, o) { return runMonteCarloSimulation(n, o); },
    applyTheme(name) { return applyTheme(name); },
    applyAccentColor(c) { return applyAccentColor(c); },
    updateQuickInfo() { return updateQuickInfo(); },
    applyQuickInfoVisibility() { return applyQuickInfoVisibility(); },
    renderSubjectTabs() { return renderSubjectTabs(); },
    updateHeatmapSubjectSelect() { return updateHeatmapSubjectSelect(); },
    updateHeatmapSubjectVisibility() { return updateHeatmapSubjectVisibility(); },
    updateHeatmapLegendLabels() { return updateHeatmapLegendLabels(); },
    checkStaleListEntries() { return checkStaleListEntries(); },
    applyGlobalSettings() { return applyGlobalSettings(); },
    setButtonLoading(id, loading) { return setButtonLoading(id, loading); },
    applyDoorPosition() { return applyDoorPosition(); },
    togglePerspective() { return togglePerspective(); },
    showPreviewModal() { return showPreviewModal(); },
    generatePreview() { return generatePreview(); },
    downloadPreview() { return downloadPreview(); },
    printSeats() { return printSeats(); },
    printParentView() { return printParentView(); },
    getTimestamp() { return getTimestamp(); },
    openHelpTab(name) { return openHelpTab(name); },
    showContextMenu(e, seat) { return showContextMenu(e, seat); },
    showColumnContextMenu(e, col) { return showColumnContextMenu(e, col); },
    handleDrop(src, tgt) { return handleDrop(src, tgt); },
    updateStats() { return updateStats(); },
    updateProbabilityPanel() { return updateProbabilityPanel(); },
    bindEvents() { return bindEvents(); },
    bindKeyboardShortcuts() { return bindKeyboardShortcuts(); },
    _renderCmdResults(q) { return _renderCmdResults(q); },
};
